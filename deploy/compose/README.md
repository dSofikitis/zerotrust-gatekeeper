# deploy/compose

Full local stack via Docker Compose: gateway, auth-issuer,
backend-echo, OPA, Redis, Prometheus, Grafana.

Lands in phase 2 (datastores + observability first), then services
join one phase at a time.
